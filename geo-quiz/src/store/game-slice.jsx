import { createSlice } from "@reduxjs/toolkit";

const gameSlice = createSlice({
    name: 'game',
    initialState: {

    },
    reducers: {

    }
});

export const sceneActions = gameSlice.actions;

export default gameSlice;